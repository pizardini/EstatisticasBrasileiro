import model.Carregamento.Carregamento;
import model.GUI.GUIFrame;

public class GUIMain {
    public static void main(String[] args) {
        Carregamento carregamento = new Carregamento();
        GUIFrame frame = new GUIFrame();
    }
}
